---
// src/pages/recetas.astro
import Layout from "../layout/Layout.astro";
import recetasData from "../data/recetas.json";

// Organizamos las recetas por grupos de edad
const gruposEdad = {
  "6 a 8 meses": recetasData.recetas.filter((r) => r.edad === "6-8"),
  "9 a 11 meses": recetasData.recetas.filter((r) => r.edad === "9-11"),
  "12 a 23 meses": recetasData.recetas.filter((r) => r.edad === "12-23"),
};
---

<Layout title="Recetas Contra la Anemia Infantil | NutriKids">
  <main class="max-w-7xl mx-auto py-8 px-4 sm:px-6 lg:px-8">
    <!-- Hero Section Mejorada -->
    <section class="text-center mb-12 bg-gradient-to-br from-amber-400 to-orange-400 rounded-2xl p-8 sm:p-12 text-white relative overflow-hidden">
      <div class="absolute inset-0 opacity-10">
        <div class="absolute inset-0 bg-[url('data:image/svg+xml;base64,')]"></div>
      </div>
      <div class="relative z-10">
        <div class="inline-flex bg-white/20 backdrop-blur-sm rounded-full px-5 py-2 mb-6">
          <span class="text-white font-bold uppercase tracking-wider text-sm">Alimentación Saludable</span>
        </div>
        <h1 class="text-3xl sm:text-4xl md:text-5xl font-bold mb-4 leading-tight">
          Deliciosas Recetas <span class="text-amber-100">Contra la Anemia</span>
        </h1>
        <p class="text-lg sm:text-xl text-amber-100 max-w-3xl mx-auto mb-8">
          Alimentos ricos en hierro adaptados a cada etapa de desarrollo de tu bebé
        </p>
        <div class="flex flex-wrap justify-center gap-3">
          {
            Object.keys(gruposEdad).map((edad) => (
              <a
                href={`#${edad.replace(/\s+/g, "-")}`}
                class="px-5 py-2 rounded-full text-sm font-medium bg-white/90 text-orange-600 
                     hover:bg-white hover:shadow-md transition-all"
              >
                {edad}
              </a>
            ))
          }
        </div>
      </div>
    </section>

    <!-- Sección por grupos de edad -->
    {
      Object.entries(gruposEdad).map(([edad, recetas]) => (
        <section id={edad.replace(/\s+/g, "-")} class="mb-16 scroll-mt-20">
          <!-- Encabezado mejorado -->
          <div class="flex flex-col sm:flex-row items-start sm:items-center mb-8 p-4 bg-white rounded-xl shadow-sm">
            <div class="mr-4 p-3 rounded-lg bg-amber-100 text-amber-600 mb-4 sm:mb-0">
              <span class="text-2xl">
                {edad === "6 a 8 meses" && "👶"}
                {edad === "9 a 11 meses" && "🍼"}
                {edad === "12 a 23 meses" && "🧒"}
              </span>
            </div>
            <div>
              <h2 class="text-xl sm:text-2xl font-bold text-gray-900">
                Recetas para {edad}
              </h2>
              <p class="text-gray-600">
                {edad === "6 a 8 meses" && "Primeras comidas - purés suaves y fáciles de digerir"}
                {edad === "9 a 11 meses" && "Texturas más gruesas - estimulación de la masticación"}
                {edad === "12 a 23 meses" && "Alimentos familiares - dieta variada y nutritiva"}
              </p>
            </div>
          </div>

          {recetas.length > 0 ? (
            <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
              {recetas.map((receta) => (
                <article
                  class="bg-white rounded-2xl overflow-hidden shadow-md hover:shadow-lg transition-shadow 
                        border border-gray-100 hover:border-amber-200 flex flex-col h-full transform hover:-translate-y-1 transition-transform"
                >
                  {/* Imagen con efecto mejorado */}
                  <div class="relative aspect-[4/3] overflow-hidden">
                    <img
                      src={receta.imagen}
                      alt={receta.titulo}
                      class="w-full h-full object-cover transition-transform duration-500 hover:scale-110"
                      loading="lazy"
                    />
                    <div class="absolute inset-0 bg-gradient-to-t from-black/40 via-black/10 to-transparent" />
                    <span class="absolute top-3 right-3 bg-white/90 text-gray-800 text-xs font-bold px-3 py-1 rounded-full shadow-sm flex items-center">
                      ⏱️ <span class="ml-1">{receta.tiempo}</span>
                    </span>
                    <span class="absolute bottom-3 left-3 bg-amber-500 text-white text-xs font-bold px-3 py-1 rounded-full shadow-sm">
                      {receta.edad === "6-8" ? "6-8 meses" : 
                       receta.edad === "9-11" ? "9-11 meses" : "1-2 años"}
                    </span>
                  </div>

                  {/* Contenido mejorado */}
                  <div class="p-5 flex-grow flex flex-col">
                    <div class="flex justify-between items-start mb-3">
                      <h3 class="text-lg font-bold text-gray-900">
                        {receta.titulo}
                      </h3>
                      <span class="text-xs text-gray-600 bg-gray-50 px-2 py-1 rounded-full flex items-center">
                        🍽️ <span class="ml-1">{receta.porciones}</span>
                      </span>
                    </div>

                    <p class="text-gray-600 text-sm mb-4 line-clamp-2 flex-grow">
                      {receta.descripcion}
                    </p>

                    {/* Nutrientes destacados mejorados */}
                    <div class="mb-4">
                      <h4 class="text-xs font-semibold text-gray-500 mb-2 uppercase tracking-wider">
                        Nutrientes Clave
                      </h4>
                      <div class="flex flex-wrap gap-2">
                        {receta.nutrientes?.map((nutriente) => (
                          <span class="text-xs px-3 py-1 rounded-full bg-amber-50 text-amber-700 border border-amber-100">
                            {nutriente}
                          </span>
                        ))}
                      </div>
                    </div>

                    <a
                      href={`/recetas/${receta.id}`}
                      class="mt-auto text-sm font-medium inline-flex items-center justify-center
                          text-white bg-amber-500 hover:bg-amber-600 transition-colors
                          group px-4 py-2 rounded-lg hover:shadow-md w-full text-center"
                    >
                      Ver receta completa
                      <svg
                        xmlns="http://www.w3.org/2000/svg"
                        class="ml-2 h-4 w-4 transition-transform group-hover:translate-x-1"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke="currentColor"
                      >
                        <path
                          stroke-linecap="round"
                          stroke-linejoin="round"
                          stroke-width="2"
                          d="M9 5l7 7-7 7"
                        />
                      </svg>
                    </a>
                  </div>
                </article>
              ))}
            </div>
          ) : (
            <div class="bg-amber-50 rounded-xl p-8 text-center border-2 border-amber-100 border-dashed">
              <span class="text-4xl block mb-4">🍎</span>
              <h3 class="text-lg font-medium text-gray-900 mt-4">
                Próximamente más recetas
              </h3>
              <p class="text-gray-600 mt-2 max-w-md mx-auto">
                Estamos trabajando en nuevas recetas especialmente diseñadas para este grupo de edad.
              </p>
              <a href="#contacto" class="mt-4 inline-block text-sm text-amber-600 hover:underline">
                ¿Tienes alguna sugerencia?
              </a>
            </div>
          )}
        </section>
      ))
    }

    <!-- Sección de consejos mejorada -->
    <section class="mt-16 bg-gradient-to-br from-amber-50 to-orange-50 rounded-2xl p-8 shadow-inner">
      <div class="text-center mb-10">
        <h2 class="text-2xl sm:text-3xl font-bold text-gray-900 mb-3">
          Consejos para <span class="text-amber-600">Prevenir la Anemia</span>
        </h2>
        <p class="text-gray-600 max-w-2xl mx-auto">
          Recomendaciones prácticas basadas en las guías del Ministerio de Salud
        </p>
      </div>
      <div class="grid gap-6 md:grid-cols-3">
        <!-- Consejo 1 -->
        <div class="bg-white p-6 rounded-xl shadow-sm border border-amber-100 hover:shadow-md transition-shadow">
          <div class="bg-amber-100 w-12 h-12 rounded-lg flex items-center justify-center mb-4 text-amber-600">
            <span class="text-2xl">🥩</span>
          </div>
          <h3 class="font-bold text-lg text-gray-900 mb-2">Hierro Hemínico</h3>
          <p class="text-gray-600">
            Incluye carnes rojas, hígado y pescado en la dieta de tu hijo (a partir de los 6 meses). Estos alimentos contienen hierro de fácil absorción.
          </p>
        </div>
        
        <!-- Consejo 2 -->
        <div class="bg-white p-6 rounded-xl shadow-sm border border-orange-100 hover:shadow-md transition-shadow">
          <div class="bg-orange-100 w-12 h-12 rounded-lg flex items-center justify-center mb-4 text-orange-600">
            <span class="text-2xl">🍊</span>
          </div>
          <h3 class="font-bold text-lg text-gray-900 mb-2">Vitamina C</h3>
          <p class="text-gray-600">
            Combina alimentos ricos en hierro con frutas cítricas (naranja, mandarina, limón) para mejorar la absorción del hierro no hemínico.
          </p>
        </div>
        
        <!-- Consejo 3 -->
        <div class="bg-white p-6 rounded-xl shadow-sm border border-amber-100 hover:shadow-md transition-shadow">
          <div class="bg-amber-100 w-12 h-12 rounded-lg flex items-center justify-center mb-4 text-amber-600">
            <span class="text-2xl">👩‍⚕️</span>
          </div>
          <h3 class="font-bold text-lg text-gray-900 mb-2">Control Médico</h3>
          <p class="text-gray-600">
            Realiza controles periódicos de hemoglobina según lo indique tu pediatra, especialmente a los 6, 12 y 18 meses de edad.
          </p>
        </div>
      </div>
      
      <!-- Banner adicional -->
      <div class="mt-10 bg-amber-100 rounded-xl p-6 text-center border-2 border-amber-200">
        <p class="text-amber-800 font-medium">
          <span class="text-xl mr-2">💡</span> Recuerda que la prevención de la anemia comienza desde el embarazo con una alimentación adecuada de la madre.
        </p>
      </div>
    </section>
    
    <!-- Sección de suscripción -->
    <section class="mt-16 bg-gradient-to-r from-amber-400 to-orange-400 rounded-2xl p-8 text-white text-center">
      <h3 class="text-xl sm:text-2xl font-bold mb-4">¿Quieres más recetas saludables?</h3>
      <p class="mb-6 max-w-2xl mx-auto">Suscríbete para recibir nuevas recetas y consejos nutricionales cada semana.</p>
      <form class="max-w-md mx-auto flex flex-col sm:flex-row gap-3">
        <input 
          type="email" 
          placeholder="Tu correo electrónico" 
          class="flex-grow px-4 py-3 rounded-lg border-0 focus:ring-2 focus:ring-white text-gray-900"
          required
        />
        <button 
          type="submit" 
          class="px-6 py-3 bg-white text-amber-600 font-bold rounded-lg hover:bg-gray-100 transition-colors shadow-md"
        >
          Suscribirme
        </button>
      </form>
    </section>
  </main>
</Layout>